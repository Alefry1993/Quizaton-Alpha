<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/nav_graph"
    app:startDestination="@id/mainActivity">

    <activity
        android:id="@+id/mainActivity"
        android:name="com.example.quizaton_alpha.Activities.MainActivity"
        android:label="MainActivity" />
    <activity
        android:id="@+id/lagActivity"
        android:name="com.example.quizaton_alpha.Activities.lagActivity"
        android:label="lagActivity" />
    <activity
        android:id="@+id/scoreboardActivity"
        android:name="com.example.quizaton_alpha.Activities.scoreboardActivity"
        android:label="scoreboardActivity" />
    <action
        android:id="@+id/action_global_mainActivity2"
        app:destination="@id/mainActivity" />
    <action
        android:id="@+id/action_global_lagActivity"
        app:destination="@id/lagActivity" />
    <action
        android:id="@+id/action_global_scoreboardActivity"
        app:destination="@id/scoreboardActivity" />
</navigation>